# ---------------------------------------------------------------------------
# Real-time multimodal second-brain assistant — dependencies
# ---------------------------------------------------------------------------
# CORE (required). The system runs with just these; every optional dep below
# has a graceful fallback so nothing here hard-fails at import time.
# ---------------------------------------------------------------------------
groq>=1.1.1                 # LLM + Whisper STT + PlayAI TTS (single fast cloud path)
python-dotenv>=1.2.2
numpy>=1.26
requests>=2.32
sounddevice>=0.4.6         # continuous mic capture + audio playback
scipy>=1.11                # wav helpers
openai>=2.0                # OpenAI-compatible client for the HF vision router
opencv-python>=4.9         # camera + scene-change detection
pillow>=10

# ---------------------------------------------------------------------------
# RECOMMENDED — big quality/latency wins, install if you can.
# ---------------------------------------------------------------------------
anthropic>=0.40           # Claude LLM provider (LLM_PROVIDER=anthropic) — strongest reasoning
sentence-transformers>=2.6 # real semantic memory embeddings (vs hash fallback)
silero-vad>=5.0            # accurate voice-activity detection (needs torch)
torch>=2.1                 # Silero VAD + local Whisper (GPU accelerates both)

# ---------------------------------------------------------------------------
# OPTIONAL — extra backends / features (each degrades gracefully if absent).
# ---------------------------------------------------------------------------
# faster-whisper>=1.0       # low-latency LOCAL STT (offline alternative to Groq)
# openai-whisper            # simplest local STT fallback
# openwakeword>=0.6         # "Hey Jarvis" wake-word gating
# webrtcvad>=2.0            # lightweight CPU VAD (fallback if no torch)
# pyttsx3>=2.90             # fully-offline TTS fallback
# fastembed>=0.3            # fast ONNX embeddings (alt to sentence-transformers)
# faiss-cpu>=1.8            # scales the vector store beyond ~5k memories
# gradio>=4.0               # optional live dashboard UI (ui/gradio_app.py)
